import styled from "styled-components";

const StyledComponents = styled.div`
.provide-content button:hover {
   background-color: #000 !important;
   color: #fff !important;
   transition :0.4s ease;
 }

`
export default StyledComponents;